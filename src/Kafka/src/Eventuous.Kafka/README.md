# Eventuous Kafka support

This package adds support for [Kafka](https://kafka.apache.org/) to applications built with Eventuous. 
It includes the following components:

- Message producer (`KafkaProducer`)
