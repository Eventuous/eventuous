# Eventuous KurrentDB support

This package adds support for [KurrentDB](https://kurrent.io) to applications built with Eventuous.
It includes the following components:

- `KurrentDBEventStore` - implementation of the `IEventStore` interface
- Different types of subscriptions (catch-up and persistent)
- Message producer